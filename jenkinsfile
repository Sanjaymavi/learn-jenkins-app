pipeline {
    agent any

    stages {
        stage('test') {
            steps {
                        echo 'Hello World from Kubernetes!'
                    sh 'node -v'
            }
        }
    }
    
    post {
        always {
            junit 'test-result/junit.xml'
        }
    }
}