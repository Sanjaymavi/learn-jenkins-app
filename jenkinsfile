pipeline {
    agent any

    stages {
        stage('build') {
            steps {
                    echo 'Hello World'
            }
        }
        stage('test') {
            steps {
                echo 'Hello from the test'
            }
        }
    }
    
    // post {
    //     always {
    //         junit 'test-result/junit.xml'
    //     }
    // }
}